# @zootech/web

Painel ZooTech. Adapter de entrega (Next.js App Router).

Chassis: Início (`/`), Animais (`/animais`) e Recintos (`/recintos`). Telas recebem dados já resolvidos. O domínio do produto não vive aqui.
